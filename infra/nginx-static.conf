# 정적 웹 MVP 서빙 설정. /run/{slug} 하위 경로에서 서빙되므로 상대 경로 기준.
server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # 소스맵 등 개발 파일 노출 차단
    location ~ \.map$ { return 404; }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
